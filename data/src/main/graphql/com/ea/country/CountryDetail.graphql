query CountryDetails($code: ID!){
    country(code: $code) {
        name
        native
        capital
        emoji
        code
        currency
        languages {
            code
            name
        }
    }
}